from django.urls import path, include
from rest_framework.routers import DefaultRouter
from .views import HeadquartersViewSet, SoporteDocumentalViewSet

router = DefaultRouter()
router.register(r'sedes', HeadquartersViewSet)
router.register(r'soportes', SoporteDocumentalViewSet)

urlpatterns = [
    path('', include(router.urls)),
]